zuul:
  routes:
    aaa:
      path: /myProduct/**
      serviceId: product
      sensitiveHeaders:
    #简洁写法
#    product: /myProduct/**
  ignored-patterns:
    - /**/product/decreaseStock
management:
  endpoints:
    web:
      exposure:
        include: "*"
